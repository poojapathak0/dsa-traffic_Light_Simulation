#ifndef COMMON_TYPES_H
#define COMMON_TYPES_H

// Directions
typedef enum {
    NORTH,
    EAST,
    SOUTH,
    WEST
} Direction;

// Vehicle types
typedef enum {
    CAR,
    TRUCK,
    BIKE
} VehicleType;

#endif